# Frontend Environment — copy to .env.production for production builds
# Leave empty to use Vite's dev proxy (localhost:5000)

# Your backend API URL — no trailing slash, no /api suffix
# Examples:
#   http://192.168.1.100:5000
#   https://api.yourdomain.com
VITE_API_BASE_URL=http://157.173.221.226:3006/api

